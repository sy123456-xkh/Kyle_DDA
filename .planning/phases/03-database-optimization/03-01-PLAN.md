---
phase: 03-database-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/db.py
  - backend/app/services/dataset_service.py
  - backend/app/services/query_service.py
  - backend/requirements.txt
autonomous: true
requirements:
  - REQ-BE-010
  - REQ-BE-011
  - REQ-FUNC-003

must_haves:
  truths:
    - "DuckDB 连接复用"
    - "元数据持久化到数据库"
    - "查询历史可查询"
  artifacts:
    - path: "backend/app/db.py"
      provides: "连接池管理"
      min_lines: 50
  key_links:
    - from: "backend/app/services/*.py"
      to: "backend/app/db.py"
      via: "连接池使用"
      pattern: "get_conn"
---

<objective>
数据库层优化：改进连接管理，添加元数据和历史记录

Purpose: 提升性能和可维护性
Output: 优化的数据库访问层，持久化的元数据和历史
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/kc/Desktop/Kyle_Studios/DDA/.planning/PROJECT.md
@/Users/kc/Desktop/Kyle_Studios/DDA/.planning/ROADMAP.md
@/Users/kc/Desktop/Kyle_Studios/DDA/.planning/REQUIREMENTS.md
@/Users/kc/Desktop/Kyle_Studios/DDA/backend/app/db.py
@/Users/kc/Desktop/Kyle_Studios/DDA/backend/app/services/dataset_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 实现连接池管理</name>
  <files>backend/app/db.py</files>
  <action>
更新 backend/app/db.py：
- 创建全局连接池（使用 threading.local 或简单的单例模式）
- 修改 get_conn() 返回池化连接
- 添加 close_all_connections() 清理函数
- 注意：DuckDB 的连接是线程安全的，但需要确保连接复用
  </action>
  <verify>
    <automated>cd /Users/kc/Desktop/Kyle_Studios/DDA/backend && python -c "from app.db import get_conn; c1 = get_conn(); c2 = get_conn(); print('OK' if c1 == c2 else 'FAIL')"</automated>
  </verify>
  <done>连接池实现，连接可复用</done>
</task>

<task type="auto">
  <name>Task 2: 元数据表设计与实现</name>
  <files>backend/app/db.py, backend/app/services/dataset_service.py</files>
  <action>
更新 backend/app/db.py：
- 添加 init_metadata_tables() 函数创建元数据表
- 创建 datasets 表：id, filename, created_at, row_count, column_count

更新 backend/app/services/dataset_service.py：
- upload_csv() 完成后插入元数据到 datasets 表
- 移除文件系统的 manifest.json，改用数据库存储
  </action>
  <verify>
    <automated>cd /Users/kc/Desktop/Kyle_Studios/DDA/backend && python -c "from app.db import get_conn; conn = get_conn(); print('OK' if conn.execute('SELECT * FROM datasets LIMIT 1') else 'FAIL')"</automated>
  </verify>
  <done>元数据表创建，数据持久化到数据库</done>
</task>

<task type="auto">
  <name>Task 3: 查询历史记录</name>
  <files>backend/app/db.py, backend/app/services/query_service.py</files>
  <action>
更新 backend/app/db.py：
- 创建 query_history 表：id, dataset_id, question, sql, created_at, elapsed_ms

更新 backend/app/services/query_service.py：
- execute_query() 完成后插入历史记录
- 添加 get_query_history(dataset_id, limit) 函数查询历史
  </action>
  <verify>
    <automated>cd /Users/kc/Desktop/Kyle_Studios/DDA/backend && python -c "from app.services.query_service import get_query_history; print('OK')"</automated>
  </verify>
  <done>查询历史记录功能实现</done>
</task>

</tasks>

<verification>
1. 连接池测试：多次调用 get_conn() 返回同一连接
2. 元数据测试：上传 CSV 后查询 datasets 表
3. 历史记录测试：执行查询后查询 query_history 表
4. 功能测试：确保现有功能正常工作
</verification>

<success_criteria>
- 连接池正常工作（连接复用）
- 元数据持久化到数据库
- 查询历史可查询
- 现有功能不受影响
</success_criteria>

<output>
After completion, create `.planning/phases/03-database-optimization/03-01-SUMMARY.md`
</output>
