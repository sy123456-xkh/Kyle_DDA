---
phase: 08-test-coverage
plan: 02
title: 前端组件测试（Vitest）
subsystem: frontend
tags: [vitest, component-test, react-testing-library]
dependency_graph:
  requires: [08-01]
  provides: [frontend-test-suite, vitest-config]
  affects: [frontend]
estimated_tasks: 3
must_haves:
  truths:
    - Vitest 测试套件可通过 `cd frontend && npm test` 运行
    - Toast/ErrorBoundary/Skeleton 组件有测试覆盖
    - 前端测试覆盖率 ≥ 50%（组件逻辑比后端业务简单，目标适当降低）
  artifacts:
    - frontend/vitest.config.ts (新建)
    - frontend/tests/setup.ts (新建)
    - frontend/tests/components/Toast.test.tsx (新建)
    - frontend/tests/components/ErrorBoundary.test.tsx (新建)
    - frontend/tests/components/Skeleton.test.tsx (新建)
    - frontend/package.json (修改，添加 vitest/testing-library deps + test 脚本)
  key_links:
    - vitest.config.ts 配置 jsdom 环境
    - setup.ts 引入 @testing-library/jest-dom matchers
---

# Phase 8 Plan 02: 前端组件测试（Vitest）

## 目标

为前端 UX 基础组件（Toast、ErrorBoundary、Skeleton）编写 Vitest 组件测试。

## 前置条件

Plan 01 完成（后端测试通过）。

## 当前状态

- 无任何前端测试文件
- package.json 无 vitest 相关依赖
- 已有组件：Toast.tsx、ErrorBoundary.tsx、Skeleton.tsx、ChartView.tsx、ChartConfig.tsx

---

## Task 1: Vitest 配置

**创建** `frontend/vitest.config.ts`
**创建** `frontend/tests/setup.ts`
**修改** `frontend/package.json`

vitest.config.ts：
```typescript
import { defineConfig } from 'vitest/config'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react()],
  test: {
    environment: 'jsdom',
    setupFiles: ['./tests/setup.ts'],
    globals: true,
    coverage: {
      provider: 'v8',
      reporter: ['text', 'json'],
      include: ['app/components/**/*.tsx'],
    },
  },
})
```

tests/setup.ts：
```typescript
import '@testing-library/jest-dom'
```

package.json 新增 devDependencies：
```json
"vitest": "^2.0.0",
"@vitest/coverage-v8": "^2.0.0",
"@testing-library/react": "^16.1.0",
"@testing-library/jest-dom": "^6.0.0",
"@vitejs/plugin-react": "^4.0.0",
"jsdom": "^25.0.0"
```

package.json 新增 scripts：
```json
"test": "vitest run",
"test:watch": "vitest",
"test:coverage": "vitest run --coverage"
```

验证：
- `cd frontend && npm install` 成功
- `npm test` 可运行（即使无测试文件，vitest 应显示"no tests found"而非报错）

完成标准：
- vitest.config.ts 存在
- `npm test` 可运行

---

## Task 2: Toast + ErrorBoundary 组件测试

**创建** `frontend/tests/components/Toast.test.tsx`
**创建** `frontend/tests/components/ErrorBoundary.test.tsx`

Toast.test.tsx 测试用例：
```typescript
// 使用 render + act + waitFor
// - ToastProvider 渲染不报错
// - useToast().show('msg', 'success') 后，屏幕上出现该消息
// - useToast().show('err', 'error') 后，出现 error 样式 toast
// - toast 显示后 3 秒自动消失（使用 vi.useFakeTimers 加速）
// - 点击关闭按钮立即消失
```

ErrorBoundary.test.tsx 测试用例：
```typescript
// - 正常子组件正常渲染
// - 子组件抛错时显示 fallback UI（包含"出了点问题"字样）
// - 点击重试按钮后重新渲染子组件
// 辅助：创建 ThrowError 组件（props.shouldThrow ? throw new Error() : render children）
```

验证：
- `npm test tests/components/Toast.test.tsx` 通过
- `npm test tests/components/ErrorBoundary.test.tsx` 通过

完成标准：
- Toast 4 个用例通过
- ErrorBoundary 3 个用例通过

---

## Task 3: Skeleton 测试 + 覆盖率

**创建** `frontend/tests/components/Skeleton.test.tsx`

Skeleton.test.tsx 测试用例：
```typescript
// SkeletonLine
// - 渲染不报错
// - 有 animate-pulse class
// - 默认宽度 100%
// - 可自定义 width prop

// SkeletonCard
// - 渲染 3 个占位块
// - 有 animate-pulse class

// SkeletonTable
// - 渲染表格结构（行/列）
// - 有 animate-pulse class
```

运行覆盖率：
- `cd frontend && npm run test:coverage`
- 目标：components/ 目录覆盖率 ≥ 50%

验证：
- 所有 Skeleton 测试通过
- 覆盖率报告输出

完成标准：
- Skeleton 测试全部通过
- `npm run test:coverage` 显示覆盖率 ≥ 50%（components 目录）
