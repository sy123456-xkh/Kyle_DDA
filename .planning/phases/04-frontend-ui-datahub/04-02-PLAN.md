---
phase: 04-frontend-ui-datahub
plan: 02
type: execute
wave: 1
depends_on: [04-01]
files_modified:
  - frontend/tsconfig.json
  - frontend/app/contexts/DataContext.tsx
  - frontend/lib/api.ts
  - frontend/types/index.ts
  - frontend/app/data-hub/page.tsx
autonomous: true
requirements:
  - REQ-FE-001
  - REQ-FE-004
  - REQ-FE-006

must_haves:
  truths:
    - "TypeScript strict mode 启用"
    - "DataContext 提供全局状态"
    - "API 调用统一封装"
  artifacts:
    - path: "frontend/app/contexts/DataContext.tsx"
      provides: "React Context 状态管理"
      min_lines: 40
    - path: "frontend/lib/api.ts"
      provides: "统一 API 客户端"
      min_lines: 30
---

<objective>
补充 Phase 4 架构基础：TypeScript strict + React Context + API 层封装

Purpose: 完成 Phase 4 的架构重构目标，消除技术债务
Output: 类型安全的状态管理和 API 层
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@/Users/kc/Desktop/Kyle_Studios/DDA/.planning/PROJECT.md
@/Users/kc/Desktop/Kyle_Studios/DDA/.planning/REQUIREMENTS.md
@/Users/kc/Desktop/Kyle_Studios/DDA/.planning/phases/04-frontend-ui-datahub/04-REVIEWS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建类型定义文件</name>
  <files>frontend/types/index.ts</files>
  <action>
创建 types/index.ts：
- DataProfile 接口（row_count, columns, missing_rate, sample_values）
- Column 接口（name, type）
- MissingRate 接口（name, missing_rate）
- SampleValues 接口（name, values）
- ApiResponse 泛型类型
- ApiError 接口
  </action>
  <verify>
    <manual>类型定义完整，覆盖所有 API 响应</manual>
  </verify>
  <done>类型定义文件创建完成</done>
</task>

<task type="auto">
  <name>Task 2: 创建 API 客户端层</name>
  <files>frontend/lib/api.ts</files>
  <action>
创建 lib/api.ts：
- 基础配置（API_BASE_URL）
- uploadDataset(file: File): Promise<{dataset_id: string}>
- getProfile(datasetId: string): Promise<DataProfile>
- 统一错误处理（try-catch + 友好错误消息）
- 类型安全的响应解析
  </action>
  <verify>
    <manual>API 客户端可正常调用后端接口</manual>
  </verify>
  <done>API 客户端层完成</done>
</task>

<task type="auto">
  <name>Task 3: 创建 DataContext 状态管理</name>
  <files>frontend/app/contexts/DataContext.tsx</files>
  <action>
创建 contexts/DataContext.tsx：
- DataContextType 接口（datasetId, profile, isLoading, error）
- DataProvider 组件
- useData hook
- setDataset, clearDataset 方法
- 状态持久化到 localStorage（可选）
  </action>
  <verify>
    <manual>Context 可在组件树中正常使用</manual>
  </verify>
  <done>DataContext 创建完成</done>
</task>

<task type="auto">
  <name>Task 4: 启用 TypeScript strict mode</name>
  <files>frontend/tsconfig.json</files>
  <action>
更新 tsconfig.json：
- 设置 "strict": true
- 设置 "noImplicitAny": true
- 设置 "strictNullChecks": true
- 修复所有类型错误
  </action>
  <verify>
    <auto>npm run build 无类型错误</auto>
  </verify>
  <done>TypeScript strict mode 启用</done>
</task>

<task type="auto">
  <name>Task 5: 重构 Data Hub 页面使用新架构</name>
  <files>frontend/app/data-hub/page.tsx</files>
  <action>
更新 data-hub/page.tsx：
- 用 DataProvider 包裹组件
- 使用 useData hook 替代 props 传递
- 使用 api.uploadDataset 替代直接 fetch
- 使用 api.getProfile 获取数据概览
- 添加错误处理和加载状态
  </action>
  <verify>
    <manual>页面功能正常，无 props drilling</manual>
  </verify>
  <done>Data Hub 页面重构完成</done>
</task>

</tasks>

<verification>
1. TypeScript strict 编译通过（npm run build）
2. DataContext 在组件间共享状态
3. API 调用统一通过 api.ts
4. 无 props drilling
5. 错误处理完善
</verification>

<success_criteria>
- TypeScript strict mode 启用且无错误
- 状态管理通过 Context 实现
- API 调用统一封装
- 所有组件类型安全
</success_criteria>

<output>
After completion, create `.planning/phases/04-frontend-ui-datahub/04-02-SUMMARY.md`
</output>
