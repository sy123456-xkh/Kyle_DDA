---
phase: 01-backend-architecture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/config.py
  - backend/app/exceptions.py
  - backend/app/main.py
  - backend/app/schemas.py
  - backend/app/services/dataset_service.py
  - backend/app/services/query_service.py
  - backend/requirements.txt
  - backend/mypy.ini
autonomous: true
requirements:
  - REQ-BE-001
  - REQ-BE-002
  - REQ-BE-004
  - REQ-BE-005

must_haves:
  truths:
    - "所有函数有类型注解"
    - "错误响应格式统一"
    - "配置通过环境变量管理"
    - "代码通过 mypy 类型检查"
  artifacts:
    - path: "backend/app/config.py"
      provides: "配置管理类"
      min_lines: 20
    - path: "backend/app/exceptions.py"
      provides: "自定义异常类"
      min_lines: 15
    - path: "backend/mypy.ini"
      provides: "mypy 配置"
      min_lines: 5
  key_links:
    - from: "backend/app/main.py"
      to: "backend/app/exceptions.py"
      via: "异常处理器注册"
      pattern: "@app.exception_handler"
    - from: "backend/app/services/*.py"
      to: "backend/app/config.py"
      via: "配置导入"
      pattern: "from app.config import"
---

<objective>
建立后端架构基础：类型安全、统一错误处理、配置管理

Purpose: 为后续安全加固和功能扩展建立清晰的代码结构
Output: 类型安全的后端代码，通过 mypy 检查，统一的错误响应格式
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/kc/Desktop/Kyle_Studios/DDA/.planning/PROJECT.md
@/Users/kc/Desktop/Kyle_Studios/DDA/.planning/ROADMAP.md
@/Users/kc/Desktop/Kyle_Studios/DDA/.planning/REQUIREMENTS.md
@/Users/kc/Desktop/Kyle_Studios/DDA/CLAUDE.md
@/Users/kc/Desktop/Kyle_Studios/DDA/backend/app/main.py
@/Users/kc/Desktop/Kyle_Studios/DDA/backend/app/schemas.py
@/Users/kc/Desktop/Kyle_Studios/DDA/backend/app/db.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建配置管理和自定义异常</name>
  <files>backend/app/config.py, backend/app/exceptions.py</files>
  <action>
创建 backend/app/config.py：
- 使用 pydantic-settings 的 BaseSettings
- 定义环境变量：DATA_DIR（默认 backend/data）、DB_PATH（默认 backend/data/app.duckdb）、MAX_FILE_SIZE_MB（默认 100）
- 创建全局 settings 实例

创建 backend/app/exceptions.py：
- 定义自定义异常类：DatasetNotFoundError、SQLGuardrailError、FileValidationError
- 每个异常包含 message 和可选的 detail 字段
- 所有异常继承自 Exception
  </action>
  <verify>
    <automated>cd /Users/kc/Desktop/Kyle_Studios/DDA/backend && python -c "from app.config import settings; from app.exceptions import DatasetNotFoundError; print('OK')"</automated>
  </verify>
  <done>配置类和异常类可成功导入，settings 包含所需字段</done>
</task>

<task type="auto">
  <name>Task 2: 添加类型注解和统一错误处理</name>
  <files>backend/app/main.py, backend/app/services/dataset_service.py, backend/app/services/query_service.py</files>
  <action>
更新 backend/app/main.py：
- 导入自定义异常类
- 注册全局异常处理器：@app.exception_handler(DatasetNotFoundError) 返回 404、@app.exception_handler(SQLGuardrailError) 返回 403、@app.exception_handler(FileValidationError) 返回 400
- 所有路由函数添加完整类型注解（参数和返回值）
- 将现有的 try-except 改为抛出自定义异常

更新 backend/app/services/dataset_service.py：
- 所有函数添加类型注解（参数、返回值）
- 将 ValueError 改为 DatasetNotFoundError
- 使用 settings.DATA_DIR 替代硬编码路径

更新 backend/app/services/query_service.py：
- 所有函数添加类型注解
- 将 ValueError("SQL 护栏拦截") 改为 SQLGuardrailError
- 保持现有逻辑不变
  </action>
  <verify>
    <automated>cd /Users/kc/Desktop/Kyle_Studios/DDA/backend && python -c "from app.main import app; from app.services.dataset_service import upload_csv; from app.services.query_service import execute_query; print('OK')"</automated>
  </verify>
  <done>所有函数有类型注解，自定义异常正确抛出，异常处理器已注册</done>
</task>

<task type="auto">
  <name>Task 3: 配置 mypy 并验证类型检查</name>
  <files>backend/mypy.ini, backend/requirements.txt</files>
  <action>
创建 backend/mypy.ini：
- [mypy] section
- python_version = 3.11
- warn_return_any = True
- warn_unused_configs = True
- disallow_untyped_defs = True
- 排除 .venv 目录

更新 backend/requirements.txt：
- 添加 mypy
- 添加 pydantic-settings

运行类型检查验证：mypy app/
  </action>
  <verify>
    <automated>cd /Users/kc/Desktop/Kyle_Studios/DDA/backend && mypy app/ --config-file mypy.ini</automated>
  </verify>
  <done>mypy 类型检查通过，无类型错误</done>
</task>

</tasks>

<verification>
1. 运行 mypy 类型检查：cd backend && mypy app/
2. 启动服务器：cd backend && uvicorn app.main:app --reload
3. 测试错误响应格式：curl http://localhost:8000/datasets/invalid_id/profile
4. 验证配置加载：python -c "from app.config import settings; print(settings.DATA_DIR)"
</verification>

<success_criteria>
- mypy 类型检查通过（0 errors）
- 所有 API 端点有完整类型注解
- 错误响应格式统一（JSON 格式，包含 detail 字段）
- 配置通过环境变量管理
- 现有功能正常工作（上传、profile、query）
</success_criteria>

<output>
After completion, create `.planning/phases/01-backend-architecture/01-01-SUMMARY.md`
</output>
