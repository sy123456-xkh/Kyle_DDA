---
phase: 10-copilot-ai-datacontext-copilot-datahub-claude-api-ai-ab
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - backend/requirements.txt
  - backend/.env.example
  - backend/app/config.py
  - backend/app/schemas.py
  - backend/app/services/ai_service.py
  - backend/app/graph/agent.py
  - backend/app/main.py
autonomous: true
requirements: [D-06, D-07, D-08, D-09, D-10, D-11, D-12]

must_haves:
  truths:
    - "POST /ai/insight 返回 {insight, suggestions, ab_test} JSON"
    - "无 LLM_API_KEY 时返回规则生成的 mock 洞察，不报错"
    - "有效 LLM_API_KEY 时调用 OpenAI-compatible API（base_url 可配置）"
    - "LLM_MODEL 从环境变量读取，不写死"
    - "backend/app/graph/agent.py 存在 run_agent(state: dict) -> dict 函数骨架"
  artifacts:
    - path: "backend/app/services/ai_service.py"
      provides: "generate_insight(req) → AIInsightResponse，含 mock fallback"
      exports: ["generate_insight"]
    - path: "backend/app/graph/agent.py"
      provides: "run_agent 骨架，不引入 LangGraph 依赖"
      contains: "def run_agent"
    - path: "backend/app/schemas.py"
      provides: "AIInsightRequest, ABTestSpec, AIInsightResponse Pydantic 模型"
      contains: "AIInsightRequest"
    - path: "backend/app/main.py"
      provides: "POST /ai/insight 路由注册"
      contains: "/ai/insight"
  key_links:
    - from: "backend/app/main.py POST /ai/insight"
      to: "backend/app/services/ai_service.generate_insight"
      via: "直接调用"
      pattern: "generate_insight"
    - from: "backend/app/services/ai_service._call_llm"
      to: "openai.OpenAI(base_url=settings.llm_base_url)"
      via: "openai SDK chat.completions.create"
      pattern: "llm_base_url"
---

<objective>
新增后端 POST /ai/insight 端点，封装 OpenAI-compatible LLM 调用，无 API Key 时返回 mock 洞察。

Purpose: 为前端 Copilot 提供真实 AI 分析能力，支持 Claude/DeepSeek 等任意 OpenAI 兼容提供商，通过环境变量配置，无 key 时不阻断流程。
Output: ai_service.py（LLM 调用 + mock fallback）、agent.py（LangGraph 预留骨架）、schemas.py 新增模型、main.py 注册路由。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<interfaces>
<!-- 执行前必须读取以下文件 -->

From backend/app/config.py (当前，需扩展):
```python
class Settings(BaseSettings):
    DATA_DIR: str = ...
    DB_PATH: str = ...
    MAX_FILE_SIZE_MB: int = 100
    MAX_FILE_SIZE_BYTES: int = 100 * 1024 * 1024
    # 需新增：
    # llm_base_url: str = "https://api.deepseek.com"
    # llm_api_key: str = ""
    # llm_model: str = "deepseek-chat"
```

From backend/app/schemas.py (当前，需新增模型):
```python
# 已有：UploadResponse, ProfileResponse, QueryRequest, QueryResponse,
#       ChartSpec, PlaybookRequest, ErrorResponse, ManifestResponse 等
# 需新增：AIInsightRequest, ABTestSpec, AIInsightResponse
```

From backend/app/main.py (当前路由注册模式，参考):
```python
@app.post("/chat/query", response_model=QueryResponse)
async def api_chat_query(req: QueryRequest) -> QueryResponse:
    return execute_query(req.dataset_id, req.question)
```

From backend/app/services/query_service.py (参考服务层结构):
```python
# 服务函数直接返回 Pydantic 模型
# 使用 logger = logging.getLogger("chat-to-bi")
# 异常通过自定义 Exception 类抛出
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 扩展 config.py + requirements.txt + .env.example（per D-07, D-10）</name>
  <files>backend/app/config.py, backend/requirements.txt, backend/.env.example</files>
  <read_first>
    - backend/app/config.py — 确认 Settings 类当前字段和 Config 内部类
    - backend/requirements.txt — 确认当前依赖列表，避免重复添加
    - backend/.env.example — 确认当前内容
  </read_first>
  <action>
    1. 修改 backend/app/config.py，在 Settings 类中新增三个字段（追加到现有字段之后）：
    ```python
    # LLM 配置（OpenAI-compatible，支持 Claude/DeepSeek 等）
    llm_base_url: str = "https://api.deepseek.com"
    llm_api_key: str = ""
    llm_model: str = "deepseek-chat"
    ```
    字段名使用小写（pydantic-settings 会自动从环境变量 LLM_BASE_URL、LLM_API_KEY、LLM_MODEL 读取）。

    2. 修改 backend/requirements.txt，在文件末尾追加一行：
    ```
    openai>=1.0.0
    ```

    3. 修改 backend/.env.example，替换为：
    ```
    # Chat-to-BI MVP 后端环境变量

    # LLM 配置（OpenAI-compatible，支持 Claude/DeepSeek/其他）
    # 不填写 LLM_API_KEY 时，系统自动使用 mock 洞察（不阻断流程）
    LLM_BASE_URL=https://api.deepseek.com
    LLM_API_KEY=sk-xxx
    LLM_MODEL=deepseek-chat

    # 如需接入 Anthropic Claude（OpenAI 兼容端点）：
    # LLM_BASE_URL=https://api.anthropic.com/v1
    # LLM_API_KEY=sk-ant-xxx
    # LLM_MODEL=claude-3-5-haiku-20241022
    ```
  </action>
  <verify>
    <automated>grep -n "llm_api_key\|llm_base_url\|llm_model" /Users/kc/Desktop/Kyle_Studios/DDA/backend/app/config.py && grep "openai" /Users/kc/Desktop/Kyle_Studios/DDA/backend/requirements.txt</automated>
  </verify>
  <acceptance_criteria>
    - `grep "llm_api_key" backend/app/config.py` 有匹配
    - `grep "llm_base_url" backend/app/config.py` 有匹配
    - `grep "llm_model" backend/app/config.py` 有匹配
    - `grep "openai" backend/requirements.txt` 有匹配（格式为 `openai>=1.0.0`）
    - `grep "LLM_API_KEY" backend/.env.example` 有匹配
    - `grep "LLM_BASE_URL" backend/.env.example` 有匹配
  </acceptance_criteria>
  <done>config.py 含三个 LLM 字段，requirements.txt 含 openai>=1.0.0，.env.example 含完整 LLM 配置说明</done>
</task>

<task type="auto">
  <name>Task 2: 新增 AIInsightRequest/ABTestSpec/AIInsightResponse 到 schemas.py（per D-06）</name>
  <files>backend/app/schemas.py</files>
  <read_first>
    - backend/app/schemas.py — 确认现有模型结构，找到文件末尾追加位置
    - backend/app/services/query_service.py — 确认 ChartSpec 已在 schemas.py 中定义（AIInsightRequest 引用它）
  </read_first>
  <action>
    在 backend/app/schemas.py 文件末尾（ErrorResponse 之后）追加以下内容：

    ```python
    # ── AI Insight ──────────────────────────────────────────
    class AIInsightRequest(BaseModel):
        """AI 洞察请求：数据集 ID、用户问题、可选图表 spec 和样本数据。"""

        dataset_id: str
        question: str
        chart: Optional[ChartSpec] = None
        rows: list[dict] = []
        manifest: dict = {}


    class ABTestSpec(BaseModel):
        """A/B 测试方案规格。"""

        goal: str
        metric: str
        design: str
        duration: str


    class AIInsightResponse(BaseModel):
        """AI 洞察响应：核心结论、可执行建议和 A/B 测试方案。"""

        insight: str
        suggestions: list[str]
        ab_test: Optional[ABTestSpec] = None
    ```

    注意：`Optional` 和 `Union` 已在文件顶部 import，无需重复添加。`ChartSpec` 已在同文件定义，可直接引用。
  </action>
  <verify>
    <automated>grep -n "AIInsightRequest\|ABTestSpec\|AIInsightResponse" /Users/kc/Desktop/Kyle_Studios/DDA/backend/app/schemas.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep "class AIInsightRequest" backend/app/schemas.py` 有匹配
    - `grep "class ABTestSpec" backend/app/schemas.py` 有匹配
    - `grep "class AIInsightResponse" backend/app/schemas.py` 有匹配
    - `grep "dataset_id: str" backend/app/schemas.py` 至少 2 处（QueryRequest 和 AIInsightRequest 各一处）
    - `grep "ab_test: Optional\[ABTestSpec\]" backend/app/schemas.py` 有匹配
  </acceptance_criteria>
  <done>schemas.py 新增三个 AI 相关 Pydantic 模型，可被 ai_service.py 和 main.py 导入</done>
</task>

<task type="auto">
  <name>Task 3: 新建 ai_service.py 和 agent.py（per D-08, D-09, D-11, D-12）</name>
  <files>backend/app/services/ai_service.py, backend/app/graph/agent.py</files>
  <read_first>
    - backend/app/services/query_service.py — 参考服务层结构（logger、异常处理模式）
    - backend/app/config.py — 确认 settings.llm_api_key、settings.llm_base_url、settings.llm_model 字段名（Task 1 已添加）
    - backend/app/schemas.py — 确认 AIInsightRequest、AIInsightResponse、ABTestSpec 的字段名（Task 2 已添加）
  </read_first>
  <action>
    1. 新建 backend/app/services/ai_service.py：

    ```python
    """AI 洞察服务：调用 OpenAI-compatible LLM，无 API Key 时返回 mock 洞察。"""

    import json
    import logging

    from app.config import settings
    from app.schemas import ABTestSpec, AIInsightRequest, AIInsightResponse

    logger = logging.getLogger("chat-to-bi")


    def generate_insight(req: AIInsightRequest) -> AIInsightResponse:
        """生成 AI 洞察。无 API Key 时返回规则生成的 mock 洞察。"""
        if not settings.llm_api_key:
            logger.info("[ai_service] LLM_API_KEY 未配置，使用 mock 洞察")
            return _mock_insight(req)
        try:
            return _call_llm(req)
        except Exception as exc:
            logger.warning("[ai_service] LLM 调用失败，降级为 mock: %s", exc)
            return _mock_insight(req)


    def _mock_insight(req: AIInsightRequest) -> AIInsightResponse:
        """无 API Key 时返回基于规则的简单洞察（不阻断流程）。"""
        row_count = len(req.rows)
        col_count = len(req.rows[0]) if req.rows else 0
        return AIInsightResponse(
            insight=f"数据集包含 {row_count} 行、{col_count} 列。问题："{req.question}"。请配置 LLM_API_KEY 以获取真实 AI 分析。",
            suggestions=[
                "检查数据完整性，确认关键字段无缺失值",
                "对数值列进行描述性统计分析（均值、中位数、标准差）",
                "识别异常值并评估其对业务指标的影响",
            ],
            ab_test=ABTestSpec(
                goal="提升核心业务指标",
                metric="转化率 / GMV / 留存率（根据数据集选择）",
                design="随机分组：对照组保持现状，实验组应用优化方案",
                duration="建议 2-4 周，确保统计显著性",
            ),
        )


    def _build_prompt(req: AIInsightRequest) -> str:
        """构建发送给 LLM 的 prompt。"""
        schema = [{"name": k, "type": type(v).__name__} for k, v in (req.rows[0].items() if req.rows else {}.items())]
        sample = req.rows[:20]
        chart_info = req.chart.model_dump() if req.chart else {}
        manifest_info = req.manifest

        return f"""你是电商数据分析专家。

数据字段：{json.dumps(schema, ensure_ascii=False)}
样本数据（前20行）：{json.dumps(sample, ensure_ascii=False, default=str)}
图表结构：{json.dumps(chart_info, ensure_ascii=False)}
指标定义：{json.dumps(manifest_info, ensure_ascii=False)}
用户问题：{req.question}

请严格输出 JSON 格式（不要包含任何其他文字）：
{{
  "insight": "核心结论（1-2条，中文）",
  "suggestions": ["可执行建议1", "可执行建议2", "可执行建议3"],
  "ab_test": {{
    "goal": "测试目标",
    "metric": "核心指标",
    "design": "分组设计",
    "duration": "建议周期"
  }}
}}"""


    def _call_llm(req: AIInsightRequest) -> AIInsightResponse:
        """调用 OpenAI-compatible API 获取 AI 洞察。"""
        from openai import OpenAI

        client = OpenAI(
            api_key=settings.llm_api_key,
            base_url=settings.llm_base_url,
        )

        prompt = _build_prompt(req)
        response = client.chat.completions.create(
            model=settings.llm_model,
            messages=[{"role": "user", "content": prompt}],
            max_tokens=1024,
        )

        content = response.choices[0].message.content or ""
        logger.info("[ai_service] LLM 响应长度: %d chars", len(content))

        # 解析 JSON 响应
        # 去除可能的 markdown 代码块包裹
        content = content.strip()
        if content.startswith("```"):
            content = content.split("```")[1]
            if content.startswith("json"):
                content = content[4:]
        content = content.strip()

        data = json.loads(content)
        ab_test = None
        if "ab_test" in data and data["ab_test"]:
            ab_test = ABTestSpec(**data["ab_test"])

        return AIInsightResponse(
            insight=data.get("insight", ""),
            suggestions=data.get("suggestions", []),
            ab_test=ab_test,
        )
    ```

    2. 新建 backend/app/graph/ 目录和 backend/app/graph/agent.py：

    ```python
    """
    LangGraph Agent 预留骨架。
    当前直接调用 query_service + ai_service。
    未来替换为 LangGraph 工作流（M2 阶段）。
    不引入 langgraph 依赖。
    """


    def run_agent(state: dict) -> dict:
        """
        预留接口：未来替换为 LangGraph 工作流。
        当前直接调用服务层。

        Args:
            state: {
                "dataset_id": str,
                "question": str,
                "rows": list,
                "chart": dict | None,
                "manifest": dict
            }
        Returns:
            {"insight": str, "suggestions": list, "ab_test": dict | None}
        """
        from app.schemas import AIInsightRequest
        from app.services.ai_service import generate_insight

        req = AIInsightRequest(
            dataset_id=state.get("dataset_id", ""),
            question=state.get("question", ""),
            rows=state.get("rows", []),
            manifest=state.get("manifest", {}),
        )
        result = generate_insight(req)
        return result.model_dump()
    ```

    同时新建空的 backend/app/graph/__init__.py（空文件，使其成为 Python 包）。
  </action>
  <verify>
    <automated>grep -n "def generate_insight\|def _mock_insight\|def _call_llm" /Users/kc/Desktop/Kyle_Studios/DDA/backend/app/services/ai_service.py && grep -n "def run_agent" /Users/kc/Desktop/Kyle_Studios/DDA/backend/app/graph/agent.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep "def generate_insight" backend/app/services/ai_service.py` 有匹配
    - `grep "def _mock_insight" backend/app/services/ai_service.py` 有匹配
    - `grep "def _call_llm" backend/app/services/ai_service.py` 有匹配
    - `grep "llm_api_key" backend/app/services/ai_service.py` 有匹配（检查 settings.llm_api_key）
    - `grep "def run_agent" backend/app/graph/agent.py` 有匹配
    - `grep "langgraph" backend/app/graph/agent.py` 输出为空（不引入 LangGraph 依赖）
    - `backend/app/graph/__init__.py` 文件存在
  </acceptance_criteria>
  <done>ai_service.py 实现 generate_insight（含 mock fallback 和 LLM 调用），agent.py 提供 run_agent 骨架，均不引入 LangGraph 依赖</done>
</task>

<task type="auto">
  <name>Task 4: 在 main.py 注册 POST /ai/insight 路由（per D-06）</name>
  <files>backend/app/main.py</files>
  <read_first>
    - backend/app/main.py — 确认现有路由注册模式和 import 结构
    - backend/app/schemas.py — 确认 AIInsightRequest、AIInsightResponse 已在 Task 2 添加
    - backend/app/services/ai_service.py — 确认 generate_insight 函数签名（Task 3 已创建）
  </read_first>
  <action>
    修改 backend/app/main.py：

    1. 在现有 import 块中新增两行（追加到 schemas import 和 services import 之后）：
    ```python
    from app.schemas import (
        ...现有导入...,
        AIInsightRequest,   # 新增
        AIInsightResponse,  # 新增
    )
    from app.services.ai_service import generate_insight  # 新增
    ```

    2. 在文件末尾（`/datasets/{dataset_id}/download` 路由之后）追加新路由：
    ```python
    # ── POST /ai/insight ───────────────────────────────────
    @app.post("/ai/insight", response_model=AIInsightResponse)
    async def api_ai_insight(req: AIInsightRequest) -> AIInsightResponse:
        """调用 LLM 生成数据洞察、建议和 A/B 测试方案。无 API Key 时返回 mock 洞察。"""
        return generate_insight(req)
    ```

    注意：schemas 的 import 是一个多行 import 块，需要将 AIInsightRequest 和 AIInsightResponse 追加到该块中，不要新建单独的 import 行。
  </action>
  <verify>
    <automated>grep -n "ai/insight\|AIInsightRequest\|generate_insight" /Users/kc/Desktop/Kyle_Studios/DDA/backend/app/main.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep "ai/insight" backend/app/main.py` 有匹配（路由路径）
    - `grep "AIInsightRequest" backend/app/main.py` 有匹配（import 和函数参数）
    - `grep "AIInsightResponse" backend/app/main.py` 有匹配（import 和 response_model）
    - `grep "generate_insight" backend/app/main.py` 有匹配（import 和调用）
    - 后端启动无报错：`cd backend && python -c "from app.main import app; print('OK')"` 输出 OK
  </acceptance_criteria>
  <done>main.py 注册 POST /ai/insight 路由，调用 generate_insight，无 API Key 时自动降级为 mock</done>
</task>

</tasks>

<verification>
端到端验证（需先安装依赖）：
```bash
cd /Users/kc/Desktop/Kyle_Studios/DDA/backend
pip install openai>=1.0.0

# 验证无 API Key 时 mock 正常工作
curl -s -X POST http://127.0.0.1:8000/ai/insight \
  -H "Content-Type: application/json" \
  -d '{"dataset_id":"test","question":"数据有什么特点","rows":[{"a":1,"b":2}],"manifest":{}}' \
  | python3 -m json.tool
```
预期响应包含 `insight`、`suggestions`（3 条）、`ab_test` 字段。

Python 导入验证：
```bash
cd /Users/kc/Desktop/Kyle_Studios/DDA/backend
python -c "from app.services.ai_service import generate_insight; print('ai_service OK')"
python -c "from app.graph.agent import run_agent; print('agent OK')"
python -c "from app.main import app; print('main OK')"
```
</verification>

<success_criteria>
- POST /ai/insight 返回 200 + {insight, suggestions, ab_test}（无 API Key 时为 mock）
- backend/app/services/ai_service.py 存在，含 generate_insight、_mock_insight、_call_llm
- backend/app/graph/agent.py 存在，含 run_agent，无 langgraph import
- requirements.txt 含 openai>=1.0.0
- config.py 含 llm_base_url、llm_api_key、llm_model 三个字段
</success_criteria>

<output>
完成后创建 .planning/phases/10-copilot-ai-datacontext-copilot-datahub-claude-api-ai-ab/10-02-SUMMARY.md
</output>
