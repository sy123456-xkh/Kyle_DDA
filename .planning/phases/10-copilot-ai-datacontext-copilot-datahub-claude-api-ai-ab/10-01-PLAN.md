---
phase: 10-copilot-ai-datacontext-copilot-datahub-claude-api-ai-ab
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/app/data-hub/page.tsx
  - frontend/app/layout.tsx
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-05]

must_haves:
  truths:
    - "上传 CSV 后切换到 /copilot，左侧 datasetId 和字段列表可见（DataContext 跨页面共享）"
    - "header 在 /data-hub 和 /copilot 之间切换时不重新挂载（丝滑切换）"
    - "activePage 高亮随路由自动切换：/data-hub 高亮 Data Hub，/copilot 高亮 Copilot"
    - "data-hub/page.tsx 不再包含 <DataProvider> 包裹"
    - "copilot/page.tsx 不再包含自写 <nav> 块"
  artifacts:
    - path: "frontend/app/layout.tsx"
      provides: "全局 NavWrapper（Client Component）+ DataProvider + ToastProvider"
      contains: "NavWrapper"
    - path: "frontend/app/data-hub/page.tsx"
      provides: "DataHubContent 直接 export，无 DataProvider 包裹"
      contains: "export default function DataHubPage"
  key_links:
    - from: "frontend/app/layout.tsx NavWrapper"
      to: "frontend/app/components/Navigation"
      via: "usePathname() → activePage prop"
      pattern: "usePathname"
    - from: "frontend/app/layout.tsx NavWrapper"
      to: "frontend/app/contexts/DataContext"
      via: "useData() → hasDataset prop"
      pattern: "useData"
---

<objective>
修复 DataContext 跨页面断层，并将 Navigation 移入 layout.tsx 实现全局共享 header。

Purpose: data-hub/page.tsx 的局部 DataProvider 与 layout.tsx 的全局 DataProvider 是两个独立实例，导致 data-hub 上传的数据集在 copilot 页面不可见。同时 copilot 有自写 nav，与共享 Navigation 组件不一致。
Output: layout.tsx 新增 NavWrapper（Client Component），data-hub/page.tsx 删除局部 DataProvider 包裹，copilot/page.tsx 删除自写 nav 块（nav 块删除在 Plan 03 执行，本 plan 只处理 layout + data-hub）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<interfaces>
<!-- 执行前必须读取以下文件，理解当前状态 -->

From frontend/app/layout.tsx (当前):
```tsx
// Server Component — 不能直接用 usePathname
import { ToastProvider } from "./components/Toast"
import { DataProvider } from "./contexts/DataContext"

export default function RootLayout({ children }) {
  return (
    <html lang="zh-CN">
      <body>
        <DataProvider>
          <ToastProvider>{children}</ToastProvider>
        </DataProvider>
      </body>
    </html>
  )
}
```

From frontend/app/data-hub/page.tsx (当前，需修改):
```tsx
export default function DataHubPage() {
  return (
    <DataProvider>      // ← 第 58-64 行，需删除此包裹
      <DataHubContent />
    </DataProvider>
  )
}
```

From frontend/app/components/Navigation.tsx (接口，不修改):
```tsx
interface NavigationProps {
  activePage?: "landing" | "data-hub" | "copilot"
  hasDataset?: boolean
}
export default function Navigation({ activePage = "landing", hasDataset = false })
```

From frontend/app/contexts/DataContext.tsx (接口，不修改):
```tsx
export function useData(): {
  datasetId: string | null
  profile: DataProfile | null
  isLoading: boolean
  setDataset: (id: string, profile: DataProfile) => void
  setLoading: (loading: boolean) => void
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 删除 data-hub/page.tsx 的局部 DataProvider 包裹（per D-01）</name>
  <files>frontend/app/data-hub/page.tsx</files>
  <read_first>
    - frontend/app/data-hub/page.tsx — 确认当前第 58-64 行的 DataProvider 包裹结构
    - frontend/app/contexts/DataContext.tsx — 确认 DataProvider 和 useData 的导出名称
  </read_first>
  <action>
    修改 frontend/app/data-hub/page.tsx：

    将底部 export default 从：
    ```tsx
    export default function DataHubPage() {
      return (
        <DataProvider>
          <DataHubContent />
        </DataProvider>
      )
    }
    ```
    改为：
    ```tsx
    export default function DataHubPage() {
      return <DataHubContent />
    }
    ```

    同时删除顶部 import 中的 `DataProvider`（保留 `useData`）：
    将 `import { DataProvider, useData } from "../contexts/DataContext"` 改为 `import { useData } from "../contexts/DataContext"`

    其余代码（DataHubContent 函数、所有 import、handleUpload 逻辑）保持不变。
  </action>
  <verify>
    <automated>grep -n "DataProvider" /Users/kc/Desktop/Kyle_Studios/DDA/frontend/app/data-hub/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep "DataProvider" frontend/app/data-hub/page.tsx` 输出为空（无任何匹配）
    - `grep "export default function DataHubPage" frontend/app/data-hub/page.tsx` 存在
    - `grep "return <DataHubContent" frontend/app/data-hub/page.tsx` 存在
    - `grep "useData" frontend/app/data-hub/page.tsx` 存在（useData 保留）
  </acceptance_criteria>
  <done>data-hub/page.tsx 不再有局部 DataProvider，DataHubPage 直接返回 DataHubContent，依赖 layout.tsx 的全局 DataProvider</done>
</task>

<task type="auto">
  <name>Task 2: 在 layout.tsx 中新增 NavWrapper Client Component（per D-03, D-04）</name>
  <files>frontend/app/layout.tsx</files>
  <read_first>
    - frontend/app/layout.tsx — 确认当前 Server Component 结构和现有 import
    - frontend/app/components/Navigation.tsx — 确认 NavigationProps 接口（activePage 类型、hasDataset）
    - frontend/app/contexts/DataContext.tsx — 确认 useData() 返回的 datasetId 字段名
  </read_first>
  <action>
    修改 frontend/app/layout.tsx，在文件顶部新增一个独立的 Client Component `NavWrapper`，然后在 RootLayout 的 body 内渲染它。

    完整文件内容如下：

    ```tsx
    import type { Metadata } from "next"
    import "./globals.css"
    import { ToastProvider } from "./components/Toast"
    import { DataProvider } from "./contexts/DataContext"
    import ClientLayout from "./components/ClientLayout"

    export const metadata: Metadata = {
      title: "Kyle Studios — 商业智能数据分析",
      description: "上传 CSV · 自然语言提问 · 即时洞察 | by Kyle Studios",
    }

    export default function RootLayout({ children }: { children: React.ReactNode }) {
      return (
        <html lang="zh-CN">
          <head>
            <link
              href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=JetBrains+Mono:wght@400;500&display=swap"
              rel="stylesheet"
            />
          </head>
          <body className="antialiased">
            <DataProvider>
              <ToastProvider>
                <ClientLayout>{children}</ClientLayout>
              </ToastProvider>
            </DataProvider>
          </body>
        </html>
      )
    }
    ```

    然后新建 frontend/app/components/ClientLayout.tsx：

    ```tsx
    "use client"

    import { usePathname } from "next/navigation"
    import Navigation from "./Navigation"
    import { useData } from "../contexts/DataContext"

    export default function ClientLayout({ children }: { children: React.ReactNode }) {
      const pathname = usePathname()
      const { datasetId } = useData()

      const activePage: "landing" | "data-hub" | "copilot" =
        pathname.startsWith("/copilot")
          ? "copilot"
          : pathname.startsWith("/data-hub")
            ? "data-hub"
            : "landing"

      // 首页（landing）不渲染 Navigation，各页面自行决定是否需要
      // data-hub 和 copilot 由 layout 统一渲染 Navigation
      const showNav = pathname !== "/"

      return (
        <>
          {showNav && <Navigation activePage={activePage} hasDataset={!!datasetId} />}
          {children}
        </>
      )
    }
    ```

    注意：data-hub/page.tsx 的 DataHubContent 内部仍有 `<Navigation activePage="data-hub" ...>`，Task 3 会删除它。
  </action>
  <verify>
    <automated>grep -n "ClientLayout\|NavWrapper\|usePathname" /Users/kc/Desktop/Kyle_Studios/DDA/frontend/app/layout.tsx && cat /Users/kc/Desktop/Kyle_Studios/DDA/frontend/app/components/ClientLayout.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep "ClientLayout" frontend/app/layout.tsx` 有匹配
    - `frontend/app/components/ClientLayout.tsx` 文件存在
    - `grep '"use client"' frontend/app/components/ClientLayout.tsx` 有匹配
    - `grep "usePathname" frontend/app/components/ClientLayout.tsx` 有匹配
    - `grep "useData" frontend/app/components/ClientLayout.tsx` 有匹配
    - `grep 'activePage.*copilot\|activePage.*data-hub' frontend/app/components/ClientLayout.tsx` 有匹配
    - layout.tsx 仍为 Server Component（无 "use client" 指令）：`grep '"use client"' frontend/app/layout.tsx` 输出为空
  </acceptance_criteria>
  <done>layout.tsx 通过 ClientLayout 组件统一渲染 Navigation，activePage 由 usePathname() 动态计算，hasDataset 由 useData() 提供</done>
</task>

<task type="auto">
  <name>Task 3: 删除 data-hub/page.tsx 内 DataHubContent 中的 Navigation 渲染（per D-04）</name>
  <files>frontend/app/data-hub/page.tsx</files>
  <read_first>
    - frontend/app/data-hub/page.tsx — 确认 DataHubContent 内第 35 行的 Navigation 渲染位置
  </read_first>
  <action>
    在 frontend/app/data-hub/page.tsx 的 DataHubContent 函数中，删除 `<Navigation activePage="data-hub" hasDataset={!!datasetId} />` 这一行。

    同时删除顶部 import 中的 `Navigation`：
    将 `import Navigation from "../components/Navigation"` 这行删除。

    DataHubContent 的 return 从：
    ```tsx
    return (
      <div className="min-h-screen bg-white">
        <Navigation activePage="data-hub" hasDataset={!!datasetId} />
        <main className="max-w-screen-xl mx-auto px-6 py-12">
          ...
        </main>
      </div>
    )
    ```
    改为：
    ```tsx
    return (
      <div className="min-h-screen bg-white">
        <main className="max-w-screen-xl mx-auto px-6 py-12">
          ...
        </main>
      </div>
    )
    ```

    其余内容（handleUpload、UploadZone、DataProfile、Skeleton 等）保持不变。
  </action>
  <verify>
    <automated>grep -n "Navigation" /Users/kc/Desktop/Kyle_Studios/DDA/frontend/app/data-hub/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep "Navigation" frontend/app/data-hub/page.tsx` 输出为空
    - `grep "UploadZone" frontend/app/data-hub/page.tsx` 存在（其余内容未被删除）
    - `grep "DataProfile" frontend/app/data-hub/page.tsx` 存在
  </acceptance_criteria>
  <done>data-hub/page.tsx 不再自行渲染 Navigation，由 layout.tsx 的 ClientLayout 统一提供</done>
</task>

</tasks>

<verification>
运行前端 TypeScript 编译检查：
```bash
cd /Users/kc/Desktop/Kyle_Studios/DDA/frontend && npx tsc --noEmit 2>&1 | head -30
```
预期：无类型错误（或仅有与本次修改无关的已有错误）。

手动验证路径：
1. 启动前端：`cd frontend && npm run dev`
2. 访问 http://localhost:3000/data-hub — 应看到 Navigation header，Data Hub 高亮
3. 上传一个 CSV 文件
4. 点击 Navigation 中的 Copilot 链接切换到 /copilot
5. 左侧应显示刚上传的 datasetId 和字段列表（DataContext 跨页面共享验证）
6. header 在切换时不闪烁/重新挂载
</verification>

<success_criteria>
- data-hub/page.tsx 无 DataProvider、无 Navigation import 和渲染
- frontend/app/components/ClientLayout.tsx 存在，包含 "use client"、usePathname、useData
- layout.tsx 引入 ClientLayout，仍为 Server Component
- TypeScript 编译无新增错误
</success_criteria>

<output>
完成后创建 .planning/phases/10-copilot-ai-datacontext-copilot-datahub-claude-api-ai-ab/10-01-SUMMARY.md
</output>
