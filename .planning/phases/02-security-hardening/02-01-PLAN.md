---
phase: 02-security-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/db.py
  - backend/app/services/dataset_service.py
  - backend/app/services/query_service.py
  - backend/app/main.py
  - backend/requirements.txt
autonomous: true
requirements:
  - REQ-BE-007
  - REQ-BE-008
  - REQ-BE-009

must_haves:
  truths:
    - "SQL 使用参数化查询"
    - "文件上传有大小限制"
    - "dataset_id 格式验证"
    - "SQL 注入测试通过"
  artifacts:
    - path: "backend/app/db.py"
      provides: "参数化查询辅助函数"
      min_lines: 30
  key_links:
    - from: "backend/app/services/query_service.py"
      to: "backend/app/db.py"
      via: "参数化查询"
      pattern: "execute_safe"
---

<objective>
安全加固：消除 SQL 注入风险，加强文件上传和输入验证

Purpose: 保护系统免受常见安全攻击
Output: 安全的后端代码，通过安全测试
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/kc/Desktop/Kyle_Studios/DDA/.planning/PROJECT.md
@/Users/kc/Desktop/Kyle_Studios/DDA/.planning/ROADMAP.md
@/Users/kc/Desktop/Kyle_Studios/DDA/.planning/REQUIREMENTS.md
@/Users/kc/Desktop/Kyle_Studios/DDA/backend/app/db.py
@/Users/kc/Desktop/Kyle_Studios/DDA/backend/app/services/query_service.py
@/Users/kc/Desktop/Kyle_Studios/DDA/backend/app/services/dataset_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 实现 SQL 参数化查询</name>
  <files>backend/app/db.py, backend/app/services/query_service.py</files>
  <action>
更新 backend/app/db.py：
- 添加 execute_safe() 函数：接受 SQL 模板和参数，使用 DuckDB 参数化查询
- 添加 build_safe_query() 辅助函数：构建安全的 WHERE 子句

更新 backend/app/services/query_service.py：
- 修改 _execute_sql() 使用参数化查询（对于用户输入的部分）
- 保持现有的 SQL Guardrails（作为第二层防护）
- 注意：DuckDB 的 read_csv_auto 和视图名称仍使用字符串拼接（这些是内部控制的，不是用户输入）
  </action>
  <verify>
    <automated>cd /Users/kc/Desktop/Kyle_Studios/DDA/backend && python -c "from app.db import execute_safe; print('OK')"</automated>
  </verify>
  <done>参数化查询函数可用，query_service 使用安全查询</done>
</task>

<task type="auto">
  <name>Task 2: 文件上传安全验证</name>
  <files>backend/app/main.py, backend/app/services/dataset_service.py, backend/app/config.py</files>
  <action>
更新 backend/app/config.py：
- 添加 MAX_FILE_SIZE_BYTES 配置（默认 100MB）
- 添加 ALLOWED_MIME_TYPES 配置

更新 backend/app/main.py (api_upload_dataset)：
- 检查文件大小（file.size）
- 验证 MIME type（使用 python-magic 或简单的内容检查）

更新 backend/app/services/dataset_service.py (upload_csv)：
- 添加 CSV 内容验证（检查是否为有效 CSV）
- 清理文件名（防止路径遍历）
  </action>
  <verify>
    <automated>cd /Users/kc/Desktop/Kyle_Studios/DDA/backend && python -c "from app.config import settings; print(settings.MAX_FILE_SIZE_BYTES)"</automated>
  </verify>
  <done>文件上传有大小限制、类型验证、内容检查</done>
</task>

<task type="auto">
  <name>Task 3: 输入参数验证</name>
  <files>backend/app/services/dataset_service.py, backend/app/services/query_service.py</files>
  <action>
更新 backend/app/services/dataset_service.py：
- 添加 _validate_dataset_id() 函数：验证 dataset_id 格式（仅字母数字下划线，以 ds_ 开头）
- 在所有使用 dataset_id 的函数中调用验证

更新 backend/app/services/query_service.py：
- 增强 SQL Guardrails：检查更多危险模式
- 添加查询长度限制
- 验证 LIMIT 值范围
  </action>
  <verify>
    <automated>cd /Users/kc/Desktop/Kyle_Studios/DDA/backend && python -c "from app.services.dataset_service import _validate_dataset_id; _validate_dataset_id('ds_abc123'); print('OK')"</automated>
  </verify>
  <done>dataset_id 格式验证，SQL Guardrails 增强</done>
</task>

</tasks>

<verification>
1. SQL 注入测试：尝试注入恶意 SQL（应被拦截）
2. 文件上传测试：上传超大文件、非 CSV 文件（应被拒绝）
3. 输入验证测试：使用非法 dataset_id（应返回 400）
4. 功能测试：确保正常功能不受影响
</verification>

<success_criteria>
- SQL 注入测试通过（恶意输入被拦截）
- 文件上传安全验证生效
- 所有输入参数经过验证
- 现有功能正常工作
</success_criteria>

<output>
After completion, create `.planning/phases/02-security-hardening/02-01-SUMMARY.md`
</output>
